Fs = 443392;
take_3 = audioread("Take1.wav");
t_final = (0:length(take_3)-1) / Fs;
plot(t_final,take_3);

start_ax = round(0.168812);
end_ax = round(0.190276 * Fs);

a_vowel = take_3(start_ax:end_ax);
soundsc(a_vowel);